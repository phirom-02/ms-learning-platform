spring:
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
          routes:
            - id: user-service
              uri: lb:http://USER-SERVICE
              predicates:
                - Path=/api/v1/users/**
            - id: course-service
              uri: lb:http://COURSE-SERVICE
              predicates:
                - Path=/api/v1/courses/**

server:
  port: 8081